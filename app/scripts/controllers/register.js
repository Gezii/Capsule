'use strict';

/**
 * @ngdoc function
 * @name capsuleApp.controller:RegisterCtrl
 * @description
 * # RegisterCtrl
 * Controller of the capsuleApp
 */
angular.module('capsuleApp')
  .controller('RegisterCtrl', function () {
    this.awesomeThings = [
      'HTML5 Boilerplate',
      'AngularJS',
      'Karma'
    ];
  });
